# Este archivo es un ejemplo. Crea un archivo .env.local en la raíz y añade las variables que necesites.
# No compartas tu archivo .env.local en el control de versiones.

# --- Conexión a SQL Server (para importación desde ERP) ---
# Descomenta y rellena estas líneas si usas el modo de importación por SQL.
# SQL_SERVER_USER=tu_usuario_de_solo_lectura
# SQL_SERVER_PASSWORD=tu_contraseña
# SQL_SERVER_HOST=ip_del_servidor_erp
# SQL_SERVER_DATABASE=nombre_de_la_bd
# SQL_SERVER_PORT=1433

# --- Configuración de Correo (para recuperación de contraseña) ---
# Descomenta y rellena estas líneas para habilitar el envío de correos.
# SMTP_HOST=smtp.tuproveedor.com
# SMTP_PORT=587
# SMTP_USER=tu_correo@ejemplo.com
# SMTP_PASS=tu_contraseña_de_correo
# SMTP_SECURE=true

# --- Configuración de Cookies (IMPORTANTE PARA PRODUCCIÓN EN HTTP) ---
# Si tu servidor de producción corre en HTTP (sin SSL/HTTPS) en la red LAN,
# DEBES definir esta variable como 'false' para que el login funcione.
# Si usas HTTPS, puedes ponerla en 'true' o simplemente no definirla.
# CLIC_TOOLS_COOKIE_SECURE=false
