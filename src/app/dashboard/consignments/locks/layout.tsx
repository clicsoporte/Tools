/**
 * @fileoverview Layout for the new Consignments locks page.
 */
'use client';

import React from 'react';

export default function ConsignmentsLocksLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
