
/**
 * @fileoverview Layout for the Consignments Boletas page.
 */
'use client';

import React from 'react';

export default function ConsignmentsBoletasLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
