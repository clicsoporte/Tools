/**
 * @fileoverview This page has been deprecated in favor of the unified Field Assistant.
 * It is kept to prevent broken links but renders nothing.
 */
'use client';

export default function DeprecatedReplenishmentRequestPage() {
    // This page is intentionally blank. The functionality has been moved to
    // the /field-assistant page to unify the user experience for all field tasks.
    return null;
}
