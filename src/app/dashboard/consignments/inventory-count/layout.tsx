
/**
 * @fileoverview Layout for the Consignments Inventory Count page.
 */
'use client';

import React from 'react';

export default function ConsignmentsInventoryCountLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
