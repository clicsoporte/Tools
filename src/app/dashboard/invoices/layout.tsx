/**
 * @fileoverview Layout for the Invoices and XML module.
 */
import React from 'react';

export default function InvoicesLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
