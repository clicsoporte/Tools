/**
 * @fileoverview Layout for the new Warehouse Occupancy Report page.
 */
'use client';

import type { ReactNode } from "react";

export default function OccupancyReportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
    return <>{children}</>;
}
