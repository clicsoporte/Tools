/**
 * @fileoverview Layout for the new Consignments Report page.
 */
'use client';

import type { ReactNode } from "react";

export default function ConsignmentsReportLayout({
  children,
}: {
  children: ReactNode;
}) {
    return <>{children}</>;
}
