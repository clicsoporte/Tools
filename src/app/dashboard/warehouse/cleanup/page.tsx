/**
 * @fileoverview This page is intentionally left blank.
 * It was removed but is kept as an empty file to resolve a build cache issue
 * where Next.js still expects the file to exist.
 */
'use client';

export default function DeprecatedCleanupPage() {
    // This component renders nothing.
    return null;
}
